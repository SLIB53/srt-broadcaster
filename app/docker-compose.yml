services:
  ingest:
    build: ./services/ingest
    ports:
      - "347:347/udp"
      - "347:347/tcp"
    volumes:
      - stream:/tmp
  viewer:
    build: ./services/viewer
    ports:
      - "80:80"
    volumes:
      - stream:/usr/share/nginx/html/stream:ro
volumes:
  stream: {}
